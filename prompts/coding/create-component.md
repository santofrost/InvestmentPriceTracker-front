You are a senior React developer.

Generate a React component following these rules:

TECH STACK
- React
- TypeScript
- TailwindCSS

COMPONENT RULES
- Functional component
- Use TypeScript props interface
- One component per file
- Export default component

TAILWIND RULES
- Use TailwindCSS only
- Do NOT write custom CSS
- Do NOT use inline style
- Prefer Tailwind utility classes
- Use flex/grid for layout

UI RULES
- Spacing must follow 8px scale (p-2, p-4, p-6, p-8)
- Border radius should be rounded-lg
- Buttons must follow primary/secondary styles
- Inputs must be consistent

CODE STRUCTURE

1. imports
2. props interface
3. component
4. export

OUTPUT
Return only the code.